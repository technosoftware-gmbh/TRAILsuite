---
type: day
---

```nod-period
```

Set `type` to `day`, `week`, `month`, `quarter` or `year` to match the note.
Prefer *Open today* and its siblings, which create the note for you. Nothing adds
a body: what goes in a period note is your business, and *Add to day* writes its
headings the first time an entry needs one.
