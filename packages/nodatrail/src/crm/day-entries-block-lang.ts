/**
 * The fence language NODAtrail renders inside a shared CRM Person note.
 *
 * Its own file for the reason `spending-block-lang.ts` gives: two places name
 * it and neither should be the other's source, and the failure of a mismatch is
 * silent -- a fence nothing renders looks exactly like a fence with nothing to
 * say.
 */
export const NOD_DAY_ENTRIES_BLOCK_LANG = 'nod-day-entries';
