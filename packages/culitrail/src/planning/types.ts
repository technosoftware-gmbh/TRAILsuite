/**
 * The planning area's read-time domain model.
 *
 * Nothing here is persisted. These are the shapes the meal-plan reader
 * produces from a note, rebuilt on every read, which is why they live with the
 * area rather than in `settings/types.ts` alongside the things `data.json`
 * holds.
 */
export {};
